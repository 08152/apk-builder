name: Build APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Repo holen
        uses: actions/checkout@v4

      - name: Node installieren
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Dependencies installieren
        run: npm install

      - name: APK Build starten
        run: echo "Build läuft (Platzhalter)"
